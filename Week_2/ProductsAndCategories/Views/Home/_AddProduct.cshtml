@model Association

<div class="container">
    <h3 class="mb-4">Add Category</h3>
    <form asp-action="CreateAssociation2" asp-controller="Home" method="post">
        <div class="form-group d-flex justify-content-between mb-3">
            <input type="hidden" value="@Model.CategoryId" asp-for="CategoryId">
            <span asp-validation-for="CategoryId"></span>
            <select asp-for="ProductId" class="form-select">
                <option selected hidden disabled>Pick a product..</option>
                @foreach(Product i in ViewBag.AllProducts)
                {
                    <option value="@i.ProductId">@i.Name</option>
                }
            </select>
        </div>
        <div class="d-flex justify-content-end">
            <button type="submit" class="btn btn-outline-secondary">Add</button>
        </div>
    </form>
</div>